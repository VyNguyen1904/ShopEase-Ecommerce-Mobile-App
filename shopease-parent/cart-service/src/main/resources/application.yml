spring:
  application:
    name: cart-service
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 2s
clients:
  product-service:
    url: ${PRODUCT_SERVICE_URI:http://localhost:8082}
server:
  port: 8084
management.endpoints.web.exposure.include: health,info

